language: node_js

sudo: false,

branches:
  only:
    - master
    - develop

node_js:
  - '8'

install:
  - npm i npminstall && npminstall

script:
  - npm run test

after_script:
  - npm install codecov && codecov
